# AGENTS.md

## 프로젝트 개요
- 결혼 사실을 정중히 알리는 모바일 원페이지다. 예식 안내, 오시는 길, 축의금 계좌는 의도적으로 제공하지 않는다.
- npm 워크스페이스로 구성하며, 클라이언트는 React 18 + Vite + TypeScript(strict), 서버는 Node.js 20+ + Express(ESM)를 사용한다.
- 작업 전 `CLAUDE.md`의 상세 구현·콘텐츠 지침과 관련 소스를 확인한다. 실행 안내는 `README.md`를 참고하며, 문서와 구현이 다르면 실제 코드를 확인한다.

## 구조
- `client/src/App.tsx`: 로케일 상태와 섹션 조립.
- `client/src/i18n.ts`: KR / EN / JP / CN 문구.
- `client/src/tokens.ts`: 디자인 토큰, 혼인일 및 날짜 공개 여부.
- `client/src/motion.tsx`: 스크롤과 애니메이션 유틸리티.
- `client/src/components/`: 페이지 섹션 컴포넌트.
- `server/index.js`: 방명록 API와 정적 파일 제공.
- `data/`: 실제 방명록, 미디어 및 백업 데이터.
- `design/`: 디자인 참고 자료.

## 작업 원칙
- 기존 컴포넌트 구조와 스타일 패턴을 따르고, 변경은 요청 범위에 맞춘다.
- 사용자에게 보이는 문구를 바꿀 때 네 언어를 함께 확인한다. 중국어 지원과 기존 가족 정보의 생략 의도를 유지한다.
- 날짜 공개 여부는 `DATE_HIDDEN`을 따른다. 요청 없이 숨겨진 날짜를 노출하지 않는다.
- 모바일 화면, 네이티브 스크롤, `prefers-reduced-motion` 대응을 유지한다.
- 음악은 사용자 클릭으로 시작하며 자동재생을 추가하지 않는다.
- 방명록 저장의 원자성, 오류 처리와 등록 제한을 유지한다. 실제 `data/` 파일을 테스트 데이터로 덮어쓰지 않는다.
- 콘텐츠 및 배포의 자세한 교체 지점은 `CLAUDE.md`를 따른다. 동작 변경으로 문서가 달라지면 관련 문서도 갱신한다.

## 명령과 검증
루트 디렉터리에서 실행한다.

- `npm install`: 워크스페이스 의존성 설치.
- `npm run dev`: Express(3001)와 Vite 개발 서버 실행.
- `npm run build`: 클라이언트 타입 검사(`tsc --noEmit`) 후 프로덕션 빌드.
- `npm run start`: 빌드된 클라이언트와 API 제공.
- `docker compose up -d --build`: Docker 빌드 및 기동(호스트 3002).

클라이언트 코드를 변경하면 `npm run build`로 검증한다. UI 변경은 가능한 경우 모바일 화면과 관련 로케일, 모션 설정도 확인한다. 서버 변경은 관련 API의 정상·실패 동작을 실제 데이터와 분리해 검증한다. 현재 package.json에는 별도 테스트 스크립트가 없다. 문서만 변경한 경우 내용과 경로를 확인하면 된다. 완료 시 변경 내용과 실제 수행한 검증, 수행하지 못한 검증을 간결히 알린다.
